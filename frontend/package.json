{
  "name": "prolocal-frontend",
  "version": "0.2.4-beta",
  "description": "Pro-Local Association Web Frontend (React + TypeScript)",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "esbuild tests/frontend_components.test.ts --bundle --platform=node --format=esm --packages=external --outfile=tests/dist_test.mjs --jsx=automatic && node --experimental-strip-types --test tests/dist_test.mjs tests/api_client.test.ts"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.5.0"
  },
  "author": "Pro-Local Architecture Team",
  "license": "UNLICENSED"
}
